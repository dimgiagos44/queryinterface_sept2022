! Neo4j default credentials: PORT:7687; USERNAME:neo4j; PASSWORD:test
! Change these in ./queryinterface_v4/views.py at Line 12

To run the backend:

Start Neo4j
> pip install -r requirements.txt
> python3 runserver.py

To run the frontend:

> cd ui
> npm install
> npm start